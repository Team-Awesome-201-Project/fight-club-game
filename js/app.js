
//USER INPUT FORM
let form = document.getElementById('NEED HTML');

function handleSubmit(event) {
  event.preventDefault();
  clickAllowed = parseInt(event.target.NEEDHTML.value);
  form.reset();
}

form.addEventListener('submit', handleSubmit);





//FIGHT ACTION SELECTION FORM
let form = document.getElementById('NEED HTML');

function handleSubmit(event) {
  event.preventDefault();
  clickAllowed = parseInt(event.target.NEEDHTML.value);
  form.reset();
}

form.addEventListener('submit', handleSubmit);